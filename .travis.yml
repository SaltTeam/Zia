sudo: required

before_install:
    # C++17
    - sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test
    - sudo apt-get update -qq

# Enable C++ support
language: cpp

# Compiler selection
compiler:
  - gcc

install:
  - cmake
  # C++17
  - sudo apt-get install -qq g++-6
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 90

# Build steps
script:
  - mkdir build
  - cd build
  - cmake .. && make modExample